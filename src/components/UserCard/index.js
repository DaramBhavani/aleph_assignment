import React from "react";
import { IconButton } from "../index";

const UserCard = () => {
  return (
    <div>
      <p>UserCard Component</p>
      <IconButton />
    </div>
  );
};

export default UserCard;
